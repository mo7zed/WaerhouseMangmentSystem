import { Routes } from '@angular/router';
import { LaborComponent } from './labor.component';

export const laborRoutes: Routes = [
  {
    path: '',
    component: LaborComponent,
    title: 'Labor — Tachyon WMS',
  },
];
