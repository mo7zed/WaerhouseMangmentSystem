import { Routes } from '@angular/router';
import { ShippingComponent } from './shipping.component';

export const shippingRoutes: Routes = [
  {
    path: '',
    component: ShippingComponent,
    title: 'Shipping — Tachyon WMS',
  },
];
