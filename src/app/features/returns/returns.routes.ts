import { Routes } from '@angular/router';
import { ReturnsComponent } from './returns.component';

export const returnsRoutes: Routes = [
  {
    path: '',
    component: ReturnsComponent,
    title: 'Returns — Tachyon WMS',
  },
];
